import { NamespacesPipe } from './namespaces.pipe';

describe('NamespacesPipe', () => {
  it('create an instance', () => {
    const pipe = new NamespacesPipe();
    expect(pipe).toBeTruthy();
  });
});
